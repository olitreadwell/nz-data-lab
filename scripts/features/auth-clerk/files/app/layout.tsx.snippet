// Doc-only snippet. Not auto-applied by the scaffolder.
// Copy the <ClerkProvider> wrapper into apps/web/src/app/layout.tsx.

import { ClerkProvider } from '@clerk/nextjs';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <ClerkProvider>
      <html lang='en'>
        <body>{children}</body>
      </html>
    </ClerkProvider>
  );
}
