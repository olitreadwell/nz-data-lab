/**
 * Microsites published to the public site, one at a time.
 * Add a slug here when an experiment ships; the private repo keeps the full list.
 */
export const PUBLISHED_MICROSITES: readonly string[] = ['sheep-index'];
